import {dimensions,giftSteps,giftQuestions,ratings} from './data.js';
import {gifts,scores} from './engine.js';
const root=document.querySelector('#app');
const home=()=>`<section class="welcome"><div class="welcome-copy"><span class="soft-tag">☁ 写给每一份认真靠近</span><h1>把喜欢和在意，<br><em>好好说出来。</em></h1><p>从一份懂 TA 的礼物，到一次更懂彼此的对话。<br>慢一点，也可以更靠近一点。</p><div class="welcome-actions"><a class="btn" href="#gift">开始挑礼物 <span>♡</span></a><a class="btn secondary" href="#relationship">看看我们的关系</a></div><div class="tiny-proof"><span>✦ 偏好有依据</span><span>✦ 感受无对错</span><span>✦ 分享由你决定</span></div></div><div class="welcome-art"><img src="warm-gift.png" alt="奶油色礼物盒、卡片、杯子与小花的温暖手绘插画"><span class="tape tape-one"></span><span class="tape tape-two"></span><span class="doodle doodle-heart">♡</span><span class="doodle doodle-spark">✦</span><span class="hand-note">今天，也想更懂你一点</span></div></section><div class="home-grid"><a href="#gift" class="gift-cover"><div class="card-icon">🎁</div><span class="cover-top">FOR SOMEONE SPECIAL</span><div class="cover-copy"><div class="eyebrow">送礼小助手</div><div class="cover-bottom"><div><h2>送一份「你懂我」</h2><p>从 TA 的偏好、日常与那些小细节出发，<br>找到一份刚刚好的心意。</p></div><span class="circle-link" aria-label="开始送礼测评">→</span></div></div></a><section class="relation-cover"><div class="card-icon coral">💞</div><div class="eyebrow">OUR LITTLE MOMENTS</div><h2>让靠近，有迹可循。</h2><p>听见各自的感受，也温柔地看见不同。<br>用一次认真作答，开启更好的对话。</p><div class="orbit" aria-hidden="true"><span></span><span></span></div><div class="relation-bottom"><span class="pill">6 个维度 · 单人 / 双人</span><a class="btn" href="#relationship">开始测评 <span>→</span></a></div></section></div><div class="bottom-note"><div class="note-item"><span class="note-icon">☀</span><div><strong>推荐有理由</strong><p>每个方向，都来自你提供的偏好</p></div></div><div class="note-item"><span class="note-icon">☁</span><div><strong>感受没标准</strong><p>看见相处细节，不给关系打标签</p></div></div><div class="note-item"><span class="note-icon">♡</span><div><strong>隐私由你做主</strong><p>独立作答，同意后才展示对照</p></div></div></div><div class="section-title"><div><span class="soft-tag">先偷偷看一眼</span><h2>报告会是什么样子？</h2></div><span class="muted">以下是虚构示例</span></div><div class="example-row"><button class="example" data-example="gift"><span class="initial">礼</span><div><strong>给爱阅读的 TA，一份生日心意</strong><p>预算 ¥300 · 实用与心意 · 送礼指南示例</p></div><span class="arrow">→</span></button><button class="example" data-example="relationship"><span class="initial">伴</span><div><strong>关于我们，那些值得聊一聊的事</strong><p>六维关系画像 · 七天相处建议 · 双人报告示例</p></div><span class="arrow">→</span></button></div>`;
function shell(content,active='home'){root.innerHTML=`<header><a class="brand" href="#home" aria-label="懂你一点首页"><span class="brand-mark">♡</span><span><strong>懂你一点</strong><small>A LITTLE CLOSER</small></span></a><nav aria-label="主导航"><a href="#home" class="${active==='home'?'active':''}">首页</a><a href="#gift" class="${active==='gift'?'active':''}">送礼指南</a><a href="#relationship" class="${active==='relationship'?'active':''}">关系测评</a><a href="#records" class="nav-records">我的报告 ♡</a></nav></header><main class="layout">${content}</main><footer><span>♡ 懂你一点 / A LITTLE CLOSER</span><span>愿每一次用心，都被温柔接住。 © ${new Date().getFullYear()}</span></footer>`}
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const today=()=>new Date().toLocaleDateString('sv-SE');
let giftData={},relData={answers:Array(24).fill(null),mode:'solo',name:''},giftStep=0,relStep=0,draftId=null,inviteToken=null,currentReport=null,currentPair=null,routeEpoch=0,busy=false;
const toast=message=>{const el=document.querySelector('#toast');el.textContent=message;el.classList.add('visible');clearTimeout(toast.timer);toast.timer=setTimeout(()=>el.classList.remove('visible'),3500)};
async function api(path,body,method){const r=await fetch('/api'+path,{method:method||(body?'POST':'GET'),headers:body?{'Content-Type':'application/json'}:{},...(body?{body:JSON.stringify(body)}:{})});const data=await r.json();if(!r.ok)throw new Error(data.error||'暂时无法连接，请重试');return data}
const back=()=>'<a class="back" href="#home">← 返回发现</a>';
function field(q,data){const value=data[q.id];return `<fieldset class="field"><legend>${esc(q.label)}${q.required?'':' <span class="muted">· 选填</span>'}${q.hint?`<span class="hint">${esc(q.hint)}</span>`:''}</legend>${q.type==='choice'||q.type==='multi'?`<div class="choices" role="group" aria-label="${esc(q.label)}">${q.options.map(o=>`<button type="button" class="choice ${(q.type==='multi'?value?.includes(o):value===o)?'selected':''}" aria-pressed="${q.type==='multi'?!!value?.includes(o):value===o}" data-choice="${q.id}" data-value="${esc(o)}">${esc(o)}</button>`).join('')}</div>`:`<input aria-label="${esc(q.label)}" data-field="${q.id}" type="${q.type==='date'?'date':q.type==='number'?'number':'text'}" ${q.type==='number'?'min="0" max="1000000" step="1"':''} ${q.type==='date'?`min="${today()}"`:''} maxlength="2000" placeholder="${esc(q.placeholder||'')}" value="${esc(value)}">`}</fieldset>`}
function giftForm(){shell(`${back()}<div class="workspace"><aside class="sidebar"><div class="eyebrow">A GIFT THAT GETS YOU</div><h1>关于 TA 的<br>那些小细节。</h1><p>好的礼物，始于认真了解。</p><div class="steps">${giftSteps.map((t,i)=>`<div class="step ${i===giftStep?'current':i<giftStep?'done':''}"><span class="num">${i<giftStep?'✓':i+1}</span>${t}</div>`).join('')}</div><div class="aside-note">不确定，也是一种答案。<br>我们会把已知偏好与待确认的信息分开呈现。<div class="progress"><span style="width:${(giftStep+1)*25}%"></span></div></div></aside><section class="form-panel"><div class="form-head"><h2>${giftSteps[giftStep]}</h2><small>0${giftStep+1} / 04</small></div><div id="form-error" role="alert"></div>${giftQuestions.filter(q=>q.step===giftStep).map(q=>field(q,giftData)).join('')}<div class="actions"><button class="text-link" data-action="save-gift">保存草稿</button><div class="choices">${giftStep?'<button class="btn secondary" data-action="gift-prev">上一步</button>':''}<button class="btn" data-action="gift-next">${giftStep===3?'生成我的送礼指南':'继续'} ↗</button></div></div><p class="fine">答案只保存在本浏览器，他人无法访问。请勿在共用设备上保存敏感信息。</p></section></div>`,'gift')}
function setupRelationship(){shell(`${back()}<div class="narrow"><div class="eyebrow">A CONVERSATION STARTS HERE</div><h1>从「我觉得」，到「我们聊聊」。</h1><p class="muted">24 个问题，6 个维度。根据最近四周的实际相处作答，约需 5～8 分钟。</p><div class="mode-grid"><button class="mode-card" data-mode="solo"><span class="eyebrow">01 / JUST ME</span><h3>先听听自己的感受</h3><p>独立填写，了解自己在这段关系中的体验，找到一个想聊的话题。</p><span class="text-link">开始单人测评 ↗</span></button><button class="mode-card" data-mode="duo"><span class="eyebrow">02 / TOGETHER</span><h3>让两种感受相遇</h3><p>你先填写，再邀请伴侣独立作答。双方同意后，展示关系体验对照。</p><span class="text-link">开始双人测评 ↗</span></button></div><div class="notice">这是一份帮助反思与沟通的自编问卷，不是专业心理量表，不用于诊断或预测关系结局。</div><p class="fine">双方不会看到彼此的原始答案和私人备注。完成后可以随时撤回共同报告授权。</p></div>`,'relationship')}
function relForm(){const names=['关于你们',...dimensions.map(d=>d.name),'留给自己的话'];let content='';if(relStep===0){content=`<div class="field"><label for="rel-name">你希望在报告里怎么称呼自己？</label><input id="rel-name" data-rel="name" value="${esc(relData.name)}" maxlength="40" placeholder="一个昵称就好"></div><div class="field"><label for="duration">相处多久了？</label><select id="duration" data-rel="duration"><option value="">请选择</option>${['不到 3 个月','3～12 个月','1～3 年','3 年以上'].map(s=>`<option ${relData.duration===s?'selected':''}>${s}</option>`).join('')}</select></div><div class="field"><label for="living">目前的相处状态</label><select id="living" data-rel="living"><option value="">请选择</option>${['同城，未同住','同住','异地'].map(s=>`<option ${relData.living===s?'selected':''}>${s}</option>`).join('')}</select></div><div class="field"><label for="change">最近有明显的生活变化吗？ <span class="muted">· 选填</span></label><input id="change" data-rel="change" value="${esc(relData.change)}" placeholder="例如：换工作、搬家，或最近格外忙碌"></div><div class="notice">请依据最近四周的实际感受回答；相处不足四周时，根据实际相处期间回答。</div>`}else if(relStep<=6){const d=dimensions[relStep-1];content=d.questions.map((q,n)=>{const idx=(relStep-1)*4+n;return `<fieldset class="field"><legend><span class="question-number">${String(idx+1).padStart(2,'0')}</span>${q}</legend><div class="scale" role="group" aria-label="${esc(q)}">${ratings.slice(0,5).map((r,i)=>`<button class="choice ${relData.answers[idx]===i+1?'selected':''}" data-rating="${idx}" data-value="${i+1}" aria-pressed="${relData.answers[idx]===i+1}"><strong>${i+1}</strong>${r}</button>`).join('')}</div><button class="skip ${relData.answers[idx]===0?'selected':''}" data-rating="${idx}" data-value="0" aria-pressed="${relData.answers[idx]===0}">不适用 / 暂时无法判断</button></fieldset>`}).join('')}else{content=`<div class="field"><label for="notes">最近一次感到被理解，是什么时候？<br>接下来，你最希望改善什么？ <span class="muted">· 选填</span></label><textarea id="notes" data-rel="notes" maxlength="2000" placeholder="这些话只留在你的个人报告里，不会分享给伴侣。">${esc(relData.notes)}</textarea></div>${relData.mode==='duo'?`<label class="check"><input type="checkbox" data-rel="consent" ${relData.consent?'checked':''}><span>我同意在双方完成并授权后，将我的昵称与六个维度的体验分数用于共同报告。原始答案和私人备注不共享，可随时撤回。</span></label>`:''}<div class="notice">不设置关系及格线，也不评价谁更爱谁。每个维度至少有 3 个有效回答时才会显示分数。</div><p class="fine">答案只保存在本浏览器。你可以在「我的报告」删除；清除浏览器数据后将无法找回。</p>`}shell(`${back()}<div class="workspace"><aside class="sidebar"><div class="eyebrow">${relData.mode==='duo'?'TWO VOICES, ONE CONVERSATION':'A MOMENT FOR YOURSELF'}</div><h1>认真听见，<br>自己的感受。</h1><p>${inviteToken?'你正在回应伴侣的邀请。':'不必选“应该”的答案，只需选真实的感受。'}</p><div class="steps">${names.map((n,i)=>`<div class="step ${i===relStep?'current':i<relStep?'done':''}"><span class="num">${i<relStep?'✓':i+1}</span>${n}</div>`).join('')}</div></aside><section class="form-panel"><div class="form-head"><h2>${names[relStep]}</h2><small>0${relStep+1} / 08</small></div><div id="form-error" role="alert"></div>${content}<div class="actions">${inviteToken?'<span class="fine">独立作答 · 备注仅自己可见</span>':'<button class="text-link" data-action="save-rel">保存草稿</button>'}<div class="choices">${relStep?'<button class="btn secondary" data-action="rel-prev">上一步</button>':''}<button class="btn" data-action="rel-next">${relStep===7?'查看关系体验报告':'继续'} ↗</button></div></div><div class="progress"><span style="width:${(relStep+1)*12.5}%"></span></div></section></div>`,'relationship')}
function showFormError(message){const el=document.querySelector('#form-error');if(el){el.className='error';el.textContent=message;el.scrollIntoView({block:'center',behavior:'smooth'})}else toast(message)}
function giftValid(){for(const q of giftQuestions.filter(q=>q.step===giftStep&&q.required)){if(!giftData[q.id]||Array.isArray(giftData[q.id])&&!giftData[q.id].length)throw new Error('请完成：'+q.label)}if(giftStep===3){if(!Number.isFinite(Number(giftData.budget))||Number(giftData.budget)<0||Number(giftData.budget)>1000000)throw new Error('请输入 0～1000000 之间的预算');if(giftData.date<today())throw new Error('需要日期不能早于今天')}}
function relValid(){if(relStep===0&&(!relData.name?.trim()||!relData.duration||!relData.living))throw new Error('请填写昵称、相处时长和相处状态');if(relStep>=1&&relStep<=6&&relData.answers.slice((relStep-1)*4,relStep*4).some(v=>v===null))throw new Error('请回答当前四个问题；无法判断时可以选择跳过')}
async function save(kind,complete=false){const data=kind==='gift'?giftData:relData;const result=await api(draftId?'/reports/'+draftId:'/reports',{kind,data,status:complete?'complete':'draft'},draftId?'PATCH':'POST');draftId=result.id;return result}
const reportMeta=(r,example)=>`<div class="meta"><span>${example?'示例报告 · 虚构数据':r.shared?'已授权分享的报告':new Date(r.created_at||Date.now()).toLocaleDateString('zh-CN')}</span><span>懂你一点 · ${r.kind==='gift'?'PERSONAL GIFT GUIDE':'RELATIONSHIP PORTRAIT'}</span></div>`;
function reportActions(r,example){if(example)return '<div class="report-actions"><a class="btn" href="#'+(r.kind==='gift'?'gift':'relationship')+'">制作属于自己的报告 ↗</a></div>';if(r.shared)return '';return `<div class="report-actions no-print"><button class="btn secondary" data-action="print">打印 / 保存 PDF</button>${!r.pairId?`<button class="btn secondary" data-action="share">${r.share?'复制分享链接':'创建分享链接'}</button>${r.share?'<button class="text-link" data-action="unshare">撤销分享</button>':''}`:''}<a class="text-link" href="#records">我的报告</a></div><div id="share-area"></div>`}
function giftReport(r,example=false){const a=r.data;const list=gifts(a);shell(`${back()}<article class="report"><div class="report-header"><div class="eyebrow">THE ART OF GIVING</div><h1>给${esc(a.name||'TA')}的，刚刚好的心意。</h1>${reportMeta(r,example)}</div><div class="report-intro"><h2>先了解，再挑选。</h2><p>这份${esc(a.occasion||'日常')}心意，预算上限 ¥${esc(a.budget)}。${a.evidence==='TA 明确说过'?'以你记录的明确偏好作为主要依据。':a.evidence==='我观察到的'?'根据你的观察整理，购买前可以自然地确认一次。':'目前偏好以猜测为主，建议先确认 TA 真正想要什么。'}</p></div><div class="section-title"><h2>为这份心意挑选的方向</h2><span class="badge">${list.length} 个备选方案</span></div>${list.length?`<div class="gift-results">${list.map((g,i)=>`<section class="gift-result"><span class="index">0${i+1}</span><div class="eyebrow">${i===0?'FIRST CHOICE':'ANOTHER POSSIBILITY'}</div><h2>${esc(g.name)}</h2><div class="price">¥${g.price} – ${g.high}</div><ul>${g.reasons.map(s=>`<li>${esc(s)}</li>`).join('')}</ul><p><strong>挑选时留意</strong><br>${esc(g.tip)}</p><span class="badge">${esc(g.kind)}</span></section>`).join('')}</div>`:'<div class="empty"><h2>暂时没有足够稳妥的推荐</h2><p class="muted">现有预算与限制下，候选不足。可以调整预算，或先与 TA 商量想要什么。</p></div>'}<p class="fine">以上是礼物方向与参考预算，未核验实时售价、库存和配送时间。请按总预算为包装与配送留出空间。</p><div class="profile-grid"><section class="report-block"><h2>让送出的一刻，也恰好。</h2><p>${a.setting==='公开赠送'?'你记录的偏好是公开赠送；先确认 TA 当天也愿意。':'优先选一个轻松、私下的时刻，让对方自在地回应。'}${a.surprise==='提前商量'?'选定前分享两个方向，让 TA 参与决定。':a.surprise==='不确定'?'不确定是否喜欢惊喜时，先做一次简单确认。':'保留一点小惊喜，把选择理由写在卡片上。'}</p><h3>卡片可以这样写</h3><p>“${a.memory?`想起${esc(a.memory)}，还是觉得很珍贵。`:'想用一份小小的心意，认真说声谢谢。'}希望这份礼物，能让你的日常多一点喜欢。”</p></section><section class="report-block"><h2>下单前，确认这些细节。</h2><p>需要日期：${esc(a.date)}<br>所在城市：${esc(a.city)}<br>近期需求：${esc(a.need||'尚未提供，可以先问一个自然的小问题')}<br>明确限制：${esc(a.avoid||'未提供；购买前确认有无过敏、尺寸或使用限制')}</p>${a.avoid?'<p>自由填写的限制可能包含复杂情况，请逐项核对候选是否满足；不要仅凭这份推荐直接下单。</p>':''}<p>先确认是否重复购买，以及退换和预约条件。</p></section></div>${reportActions(r,example)}${feedback(r,example)}</article>`,'gift')}
function radar(ds,partner){const cx=170,cy=154,r=100;const point=(i,f)=>[cx+Math.sin(i*Math.PI/3)*r*f,cy-Math.cos(i*Math.PI/3)*r*f];const polygon=(values)=>values.map((d,i)=>point(i,(d.score??0)/100).join(',')).join(' ');return `<svg class="radar" viewBox="0 0 340 310" role="img" aria-label="关系六维画像，具体分数列于旁边">${[.25,.5,.75,1].map(f=>`<polygon points="${ds.map((_,i)=>point(i,f).join(',')).join(' ')}" fill="none" stroke="#e4d7dd"/>`).join('')}${ds.map((d,i)=>{const p=point(i,1),l=point(i,1.28);return `<line x1="170" y1="154" x2="${p[0]}" y2="${p[1]}" stroke="#e4d7dd"/><text x="${l[0]}" y="${l[1]}" text-anchor="middle" dominant-baseline="middle" font-size="11" fill="#75616b">${d.name}</text>`}).join('')}${partner?`<polygon points="${polygon(partner)}" fill="#bd91a133" stroke="#bd91a1" stroke-width="2"/>`:''}<polygon points="${polygon(ds)}" fill="#782d4422" stroke="#782d44" stroke-width="2"/></svg>`}
function relationshipReport(r,example=false,pair=null){const a=r.data;const ds=a.computed||scores(a.answers);const other=pair?.shared?pair.partner.computed:null;const usable=ds.filter(d=>d.score!==null);const strongest=[...usable].filter(d=>d.score>=50).sort((x,y)=>y.score-x.score).slice(0,2);const needs=[...usable].sort((x,y)=>x.score-y.score)[0];const gaps=other?ds.map((d,i)=>({...d,other:other[i].score,gap:Math.abs(d.score-other[i].score)})).filter(d=>d.score!==null&&d.other!==null&&d.gap>=25).sort((x,y)=>y.gap-x.gap).slice(0,2):[];shell(`${back()}<article class="report"><div class="report-header"><div class="eyebrow">THE SPACE BETWEEN US</div><h1>${other?`${esc(a.name)}与${esc(pair.partner.name)}，听见彼此。`:`${esc(a.name||'我')}的关系体验画像。`}</h1>${reportMeta(r,example)}</div>${pair&&!example?pairPanel(pair):''}<div class="report-intro"><h2>${other?'两种感受，都值得被认真听见。':'给此刻的感受，一个清晰的轮廓。'}</h2><p>${strongest.length?`从本次回答看，你在「${esc(strongest[0].name)}」方面的体验相对较积极。${other?'双方的分数表示各自感受，不代表谁对谁错。':'这份报告仅反映你的体验，不能推断对方的想法。'}`:usable.length?'你在本次回答中表达了一些需要被重视的感受。可以先照顾自己的需要，选择一个你愿意讨论的小问题。':'目前有效回答不足以形成维度分数。你仍然可以从一次具体的相处经历开始反思。'}</p></div><section class="report-block"><div class="section-title"><h2>六个维度，看见相处的细节。</h2><span class="badge">不设关系总分</span></div><div class="legend"><span><i></i>${esc(a.name||'我的体验')}</span>${other?`<span><i class="partner"></i>${esc(pair.partner.name)}</span>`:''}</div><div class="profile-grid"><div>${ds.every(d=>d.score!==null)&&(!other||other.every(d=>d.score!==null))?radar(ds,other):'<div class="empty"><h3>保留感受里的留白</h3><p class="muted">部分维度信息不足，暂不绘制完整画像。缺失不等于零分。</p></div>'}</div><div>${ds.map((d,i)=>`<div class="score-row"><div class="score-label"><span>${d.name}</span><span>${d.score===null?'信息不足':d.score}${other?` / ${other[i].score??'信息不足'}`:''}</span></div>${d.score!==null?`<div class="bar-track"><div class="bar-fill" style="width:${d.score}%"></div></div>`:''}${other&&other[i].score!==null?`<div class="bar-track"><div class="bar-fill partner" style="width:${other[i].score}%"></div></div>`:''}<small class="fine">${other?'共同有效':'有效'}回答 ${d.count} / 4</small></div>`).join('')}</div></div><p class="fine">每个维度至少 3 个有效回答；分数 =（有效答案平均分 − 1）÷ 4 × 100。双方对照只使用共同有效的题目。</p></section><div class="profile-grid"><section class="report-block"><div class="eyebrow">KEEP THE GOOD</div><h2>值得留意的积极体验</h2>${strongest.length?strongest.map(d=>`<h3>${d.name}</h3><p>这是你本次体验相对较积极的维度。可以回想一个最近的具体时刻，向对方表达那件事对你的意义。</p>`).join(''):'<p>本次暂不归纳积极体验。留意自己的感受，按舒服的节奏思考下一步。</p>'}</section><section class="report-block"><div class="eyebrow">MAKE ROOM FOR A CONVERSATION</div><h2>${other?'值得聊一聊的差异':'一个可以开始的话题'}</h2>${gaps.length?gaps.map(g=>`<h3>${g.name} · ${g.score} / ${g.other}</h3><p>你们在这一维度的感受不同。可以问：“最近有没有一个时刻，让你特别希望我多理解你一点？”</p>`).join(''):`<p>${other?'本次未出现达到展示阈值的维度差异，这并不意味着所有体验完全一致。':needs?`关于「${needs.name}」，最近有没有一件小事，你想让对方更了解你的感受？`:'选一个让你觉得被理解或没被理解的时刻，说说当时希望获得什么回应。'}</p>`}</section></div><section class="report-block"><div class="eyebrow">A LITTLE CLOSER, IN 7 DAYS</div><h2>下一周，只试着做好一件小事。</h2><p>${needs?needs.action:'选一个双方都轻松的时刻，分享最近一次感到被理解的经历。'}</p><p>先商量一个都方便的时间。结束后问问彼此：“这次有什么帮助？下次想怎么调整？”</p></section>${a.notes&&!r.shared?`<section class="report-block"><span class="badge">仅自己可见</span><h2>留给自己的话</h2><p>${esc(a.notes)}</p></section>`:''}<p class="fine">本报告采用自编问卷与固定规则，帮助反思和沟通，不是专业心理诊断，也不预测关系结局。若表达边界让你感到不安全，优先照顾自己的安全，无需立即与对方对质。</p>${reportActions(r,example)}${feedback(r,example)}</article>`,'relationship')}
function pairPanel(p){return `<section class="report-block no-print"><h2>${p.shared?'共同报告已开启':p.partnerReady?'双方已完成，等待共同授权':'邀请 TA，也说说自己的感受。'}</h2><p>${p.shared?'你们可以分别查看体验对照。私人备注和原始答案始终独立。':'只有双方完成并同意后，才展示对照。你可以先阅读自己的报告。'}</p><label class="check"><input type="checkbox" id="pair-consent" ${p.consent?'checked':''}><span>允许将我的昵称与维度分数用于本次共同报告（可随时撤回）</span></label>${p.invite?`<div class="share-url"><input aria-label="伴侣邀请链接" readonly value="${esc(location.origin+location.pathname+'#join/'+p.invite)}"><button class="btn secondary" data-action="copy-invite">复制邀请</button></div><button class="text-link" data-action="revoke-invite">撤销旧邀请并生成新链接</button>`:''}${p.returnLink?`<div class="share-url"><input aria-label="回传给伴侣的链接" readonly value="${esc(p.returnLink)}"><button class="btn secondary" data-action="copy-return">复制回传链接</button></div><p class="fine">把这条链接发给发起邀请的 TA，在原来的设备上打开，就能看到你们的共同报告。</p>`:''}<div class="section-gap"><button class="text-link" data-action="refresh-pair">刷新伴侣进度 ↻</button></div></section>`}
function feedback(r,example){return example||r.shared?'':`<div class="report-block no-print"><h3>这份报告对你有帮助吗？</h3><div class="choices"><button class="choice" data-feedback="helpful">${r.feedback==='helpful'?'✓ ':''}有帮助</button><button class="choice" data-feedback="not-helpful">${r.feedback==='not-helpful'?'✓ ':''}还不够具体</button></div></div>`}
function giftExample(){return {kind:'gift',data:{name:'小林',occasion:'生日',relation:'伴侣',duration:'1～3 年',interests:['阅读','旅行'],values:['实用','心意'],social:'两人相处',need:'想要一盏阅读灯',evidence:'TA 明确说过',budget:'300',date:today(),city:'上海',surprise:'小惊喜',setting:'私下赠送',effort:'愿意简单搭配',memory:'那次一起逛书店的下午'}}}
function relationshipExample(){const answers=[4,4,3,4,5,4,5,4,4,3,4,4,3,3,4,3,4,4,4,4,4,3,4,3],b=[4,3,4,3,4,4,5,4,2,2,3,2,3,4,3,3,4,5,4,4,3,3,4,4];return {report:{kind:'relationship',data:{name:'林',answers}},pair:{shared:true,partner:{name:'周',computed:scores(b,answers)}}}}
async function showRecord(id,epoch){const r=await api('/reports/'+id);if(epoch!==routeEpoch)return;currentReport=r;if(r.status==='draft'){draftId=r.id;if(r.kind==='gift'){giftData=r.data;giftStep=0;giftForm()}else{relData=r.data;relStep=0;inviteToken=null;relForm()}return}let pair=null;if(r.pairId){pair=await api('/pairs/'+r.pairId);if(epoch!==routeEpoch)return;r.data=pair.data;currentPair=pair}if(r.kind==='gift')giftReport(r);else relationshipReport(r,false,pair)}
async function records(epoch){const data=await api('/reports');if(epoch!==routeEpoch)return;shell(`${back()}<div class="narrow"><div class="intro"><div><div class="eyebrow">YOUR LITTLE COLLECTION</div><h1>收好，每一次用心。</h1></div></div><p class="muted" style="margin-bottom:25px">报告只保存在本浏览器。清除浏览器数据或更换设备后无法自动找回。</p>${data.reports.length?data.reports.map(r=>`<div class="record-row"><div><h3>${esc(r.title)} <span class="badge">${r.status==='draft'?'草稿':r.kind==='gift'?'送礼指南':'关系体验'}</span></h3><p>${new Date(r.created_at).toLocaleString('zh-CN')}</p></div><div class="record-actions"><a class="text-link" href="#report/${r.id}">${r.status==='draft'?'继续填写':'查看报告'} ↗</a><button class="text-link danger" data-delete="${r.id}">删除</button></div></div>`).join(''):'<div class="empty"><div class="eyebrow">A NEW BEGINNING</div><h2>你的第一份心意，还在路上。</h2><p class="muted">完成一次问卷，或保存草稿，它就会出现在这里。</p><div class="report-actions" style="justify-content:center"><a class="btn" href="#gift">做一份送礼指南 ↗</a><a class="btn secondary" href="#relationship">探索关系体验</a></div></div>'}</div>`,'records')}
async function route(){const epoch=++routeEpoch;const [page='home',id]=location.hash.slice(1).split('/');currentPair=null;currentReport=null;try{if(page==='home'||!page)shell(home());else if(page==='gift'){giftStep=0;giftData={};draftId=null;inviteToken=null;giftForm()}else if(page==='relationship'){inviteToken=null;draftId=null;setupRelationship()}else if(page==='records'){shell('<div class="loading">正在整理你的报告…</div>','records');await records(epoch)}else if(page==='report'){shell('<div class="loading">正在打开你的报告…</div>');await showRecord(id,epoch)}else if(page==='join'){shell('<div class="loading">正在确认邀请…</div>');const invitation=await api('/invite/'+id);if(epoch!==routeEpoch)return;if(invitation.pairId){const pair=await api('/pairs/'+invitation.pairId);if(epoch!==routeEpoch)return;shell(`${back()}<div class="narrow"><h1>你已完成这份邀请</h1><p class="muted">请从我的报告查看自己的结果和共同体验。</p><a class="btn" href="#records">打开我的报告</a></div>`)}else{inviteToken=id;draftId=null;relData={answers:Array(24).fill(null),mode:'duo',name:'',consent:false};relStep=0;relForm()}}else if(page==='shared'){shell('<div class="loading">正在打开分享…</div>');const r=await api('/shared/'+id);if(epoch!==routeEpoch)return;r.shared=true;if(r.kind==='gift')giftReport(r);else relationshipReport(r)}else if(page==='joined'){shell('<div class="loading">正在打开共同报告…</div>');const r=await api('/joined/'+id);if(epoch!==routeEpoch)return;location.hash='report/'+r.id}else if(page==='example-gift')giftReport(giftExample(),true);else if(page==='example-relationship'){const x=relationshipExample();relationshipReport(x.report,true,x.pair)}else shell(`${back()}<div class="empty"><h2>没有找到这个页面</h2><a href="#home" class="btn">返回首页</a></div>`)}catch(e){if(epoch===routeEpoch)shell(`${back()}<div class="empty"><h2>暂时无法打开</h2><p class="muted">${esc(e.message)}</p><div class="report-actions" style="justify-content:center"><button class="btn" data-action="retry">重试</button><a class="btn secondary" href="#records">我的报告</a></div></div>`)}window.scrollTo(0,0)}
async function copy(text){try{await navigator.clipboard.writeText(text);toast('链接已复制')}catch{const area=document.querySelector('#share-area');if(area)area.innerHTML=`<div class="share-url"><input aria-label="分享链接" readonly value="${esc(text)}"></div>`;toast('请选中链接后复制')}}
function confirmDelete(id){const d=document.createElement('dialog');d.className='dialog';d.innerHTML='<h2>删除这份报告？</h2><p>删除后无法恢复，相关分享链接与共同报告会失效。伴侣自己的个人报告仍会保留。</p><div class="actions"><button class="btn secondary" data-cancel>取消</button><button class="btn" data-confirm>确认删除</button></div>';document.body.append(d);d.querySelector('[data-cancel]').onclick=()=>d.close();d.addEventListener('close',()=>d.remove());d.querySelector('[data-confirm]').onclick=async()=>{try{await api('/reports/'+id,null,'DELETE');d.close();toast('报告已删除');await route()}catch(e){toast(e.message)}};d.showModal()}
root.addEventListener('input',e=>{const el=e.target;if(el.dataset.field)giftData[el.dataset.field]=el.value;if(el.dataset.rel)relData[el.dataset.rel]=el.type==='checkbox'?el.checked:el.value});
root.addEventListener('change',async e=>{const el=e.target;if(el.dataset.rel)relData[el.dataset.rel]=el.type==='checkbox'?el.checked:el.value;if(el.id==='pair-consent'&&currentPair){el.disabled=true;try{await api('/pairs/'+currentPair.id,{consent:el.checked},'PATCH');await showRecord(currentReport.id,routeEpoch);toast(el.checked?'已同意共同报告':'已撤回授权，对方无法再读取你的分数')}catch(err){el.checked=!el.checked;toast(err.message)}finally{el.disabled=false}}});
root.addEventListener('click',async e=>{const button=e.target.closest('button');if(!button)return;if(busy)return;const ds=button.dataset;try{if(ds.example){location.hash='example-'+ds.example;return}if(ds.choice){const q=giftQuestions.find(q=>q.id===ds.choice);if(q.type==='multi'){let arr=giftData[q.id]||[];if(arr.includes(ds.value))arr=arr.filter(x=>x!==ds.value);else if(ds.value==='不确定')arr=['不确定'];else{arr=arr.filter(x=>x!=='不确定');if(arr.length>=(q.max||3)){toast('最多选择 '+(q.max||3)+' 项');return}arr=[...arr,ds.value]}giftData[q.id]=arr}else giftData[q.id]=ds.value;const y=window.scrollY;giftForm();root.querySelector(`[data-choice="${q.id}"][data-value="${ds.value}"]`)?.focus({preventScroll:true});window.scrollTo(0,y);return}if(ds.rating!==undefined){relData.answers[Number(ds.rating)]=Number(ds.value);const y=window.scrollY;relForm();root.querySelector(`[data-rating="${ds.rating}"][data-value="${ds.value}"]`)?.focus({preventScroll:true});window.scrollTo(0,y);return}if(ds.mode){relData={answers:Array(24).fill(null),mode:ds.mode,name:'',consent:false};draftId=null;relStep=0;relForm();return}if(ds.delete){confirmDelete(ds.delete);return}busy=true;button.disabled=true;if(ds.feedback){await api('/reports/'+currentReport.id,{feedback:ds.feedback},'PATCH');currentReport.feedback=ds.feedback;root.querySelectorAll('[data-feedback]').forEach(el=>{el.classList.toggle('selected',el.dataset.feedback===ds.feedback);el.setAttribute('aria-pressed',String(el.dataset.feedback===ds.feedback))});toast('谢谢，已记录你的反馈');return}switch(ds.action){case 'gift-prev':giftStep--;giftForm();window.scrollTo(0,0);break;case 'gift-next':giftValid();if(giftStep<3){await save('gift');giftStep++;giftForm();window.scrollTo(0,0)}else{const r=await save('gift',true);draftId=null;location.hash='report/'+r.id}break;case 'save-gift':await save('gift');toast('草稿已保存到「我的报告」');break;case 'save-rel':await save('relationship');toast('草稿已保存到「我的报告」');break;case 'rel-prev':relStep--;relForm();window.scrollTo(0,0);break;case 'rel-next':relValid();if(relStep<7){if(!inviteToken)await save('relationship');relStep++;relForm();window.scrollTo(0,0)}else{const r=inviteToken?await api('/invite/'+inviteToken,{data:relData}):await save('relationship',true);draftId=null;inviteToken=null;location.hash='report/'+r.id}break;case 'print':window.print();break;case 'retry':await route();break;case 'refresh-pair':await showRecord(currentReport.id,routeEpoch);toast('进度已更新');break;case 'copy-invite':await copy(location.origin+location.pathname+'#join/'+currentPair.invite);break;case 'revoke-invite':await api('/pairs/'+currentPair.id,{revokeInvite:true},'PATCH');await showRecord(currentReport.id,routeEpoch);toast('旧邀请已失效');break;case 'share':if(!currentReport.share){const s=await api('/reports/'+currentReport.id,{share:true},'PATCH');currentReport.share=s.share}await showRecord(currentReport.id,routeEpoch);await copy(location.origin+location.pathname+'#shared/'+currentReport.share);break;case 'unshare':await api('/reports/'+currentReport.id,{share:false},'PATCH');await showRecord(currentReport.id,routeEpoch);toast('分享链接已撤销');break;case 'copy-return':await copy(currentPair.returnLink);break}}catch(err){showFormError(err.message)}finally{busy=false;button.disabled=false}});
const mc=document.modelContext;if(mc?.registerTool){const controller=new AbortController();try{Promise.resolve(mc.registerTool({name:'navigate_closer',title:'打开懂你一点功能',description:'仅打开送礼问卷、关系测评或我的报告，不填写、不提交、不分享数据。',inputSchema:{type:'object',properties:{page:{type:'string',enum:['home','gift','relationship','records']}},required:['page'],additionalProperties:false},annotations:{readOnlyHint:false},async execute(input){if(!input||Object.keys(input).length!==1||!['home','gift','relationship','records'].includes(input.page))throw new Error('无效页面');history.replaceState(null,'','#'+input.page);await route();return {page:input.page}}},{signal:controller.signal})).catch(()=>{});window.addEventListener('pagehide',()=>controller.abort(),{once:true})}catch{}}
window.addEventListener('hashchange',route);route();
